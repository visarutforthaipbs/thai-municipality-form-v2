import { createSystem } from "@chakra-ui/react";

// Create a simple system for Chakra UI v3
// In v3, the system is significantly different from v2
// We'll keep it minimal to avoid errors
const thaiSystem = createSystem({});

export default thaiSystem;
